# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  ◈ WINDOW & LAYER RULES
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ─────────────────────────────
# Layer rules (OSD / overlays)
# ─────────────────────────────
layerrule = no_anim on, match:namespace ^(volume_osd)$
layerrule = no_anim on, match:namespace ^(brightness_osd)$
layerrule = no_anim on, match:namespace hyprpicker
layerrule = no_anim on, match:namespace qsdock
layerrule = blur on, match:namespace ext-session-lock
layerrule = ignore_alpha 0.2, match:namespace ext-session-lock

# ─────────────────────────────
# Window rules
# ─────────────────────────────

# ───────── CS2 ─────────
windowrule = immediate on, match:class ^(cs2)$
windowrule = keep_aspect_ratio on, match:class ^(cs2)$

# ───────── App Launcher ─────────
windowrule = float on, match:title ^(app-launcher)$
windowrule = center on, match:title ^(app-launcher)$
windowrule = size 1200 600, match:title ^(app-launcher)$
windowrule = animation slide, match:title ^(app-launcher)$

# ───────── MASTER QUICKSHELL CONTAINER ─────────
# All widgets now live inside this single, shape-shifting window.
# windowrule = float on, match:title ^(qs-master)$
# windowrule = no_shadow on, match:title ^(qs-master)$
# windowrule = no_border on, match:title ^(qs-master)$
# windowrule = no_initial_focus on, match:title ^(qs-master)$
